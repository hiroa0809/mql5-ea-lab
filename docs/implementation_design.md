# 実装設計書 — スパンモデル / スーパーボリンジャー

> 売買ルールは `docs/trading_rules.md`、計算定義は `docs/indicator_spec.md`。**本書はそれらを繰り返さない。**
> 本書が扱うのは「どのファイルに何を置き、どの入力で切り替え、どう執行するか」。
> 作成 2026-08-08（N2）。

## 1. ファイル構成

同じ計算をインジケーターと EA の両方が使うため、共通部分を `.mqh` に置いて両方から読み込む。2回書くと片方だけ直したときに食い違い、「チャートは合っているのにバックテストがおかしい」という切り分け困難な状態になる。

```text
Include\Signals\
  SuperBollinger.mqh   … センターライン・σ の計算 ＋ ルール1の判定
  SpanModel.mqh        … スパンA/B・遅行線の計算 ＋ ルール2の判定
  HigherTf.mqh         … 上位足の選択肢と、下位足へ並べるときの位置決め（§2.4）
  Squeeze.mqh          … 膠着の測り方5通り（trading_rules.md §6.1）
  Pivot.mqh            … ピボットの計算（indicator_spec.md §5）
  PivotDraw.mqh        … ピボットを下位足へ並べる処理。日足ぶんと週足ぶんで共有

Indicators\
  SuperBollinger.mq5      … チャート描画（N4 の目視確認用）
  SpanModel.mq5
  SuperBollingerMTF.mq5   … 上位足のスーパーボリンジャーを下位足へ重ねる（N5-8）
  SqueezeGauge.mq5        … 膠着メーター。別ウィンドウ・測り方を切替（N5-8）
  DailyPivot.mq5          … デイリーピボット7本（金色）。反転の目安を目で確かめる用
  WeeklyPivot.mq5         … ウィークリーピボット7本（水色・太め）。中身は日足ぶんと共有

Experts\
  EaSpanModel.mq5      … ルール2（スパンモデル単体）で売買。判定は SpanModel.mqh を呼ぶだけ
                         ルール1用の EA はまだ無い
```

**指標ごとに1本、計2本に分ける。** ルール1はスーパーボリンジャーだけ、ルール2はスパンモデルだけを使うので依存が自然に分かれる。`TASK_MASTER.md` の N4 が「一度に両方作らない」と決めており、1指標＝1ファイルならその作業順がそのままファイル単位になる。ルール3では両方を読み込む。

**計算と判定は同じファイルに置く。** インジケーターは描画に計算、シグナル矢印に判定が要る。EA も両方要る。利用者が両方を使う以上、分ける理由がない。

**`ISignal.mqh` のインターフェース（`Init`/`Update`/`Entry`）は使わない。** ルールが2つの段階で抽象を挟んでも得るものがない。ルール3の形が決まってから必要なら導入する（`TASK_MASTER.md`「先回りして汎用テンプレート化はしない」）。`TASK_MASTER.md` の流用候補にあった「そのまま流用できる」の記述は 2026-08-08 に取り消し済み。

### 銘柄・時間足

**`_Symbol` / `_Period` を使い、input にしない。** テスターの設定がそのまま反映され、ハードコーディングも避けられる（`docs/backtest_design.md`「選択可能にする項目」の意図を満たす）。ルール3の上位足だけは input で持つ。

### 必要な履歴本数

`OnInit` で不足を弾く。

**入力値から計算する。固定値を書かない**（期間はすべて input なので、変えた瞬間に固定値が嘘になる）。

```text
ルール1: InpR1_Period + InpR1_LagBars + max(InpR1_SqueezeBars, InpR1_ExpandBars) + 2
ルール2: max(InpR2_Tenkan, InpR2_Kijun, InpR2_SpanB) + InpR2_LagBars + InpR2_SlopeBars + 2
```

既定値では ルール1 = 65、ルール2 = 85。ルール1で最も深く遡るのは①で、膠着を見る最も古い足の帯を求めるのに `Period + LagBars + SqueezeBars`（既定 63）本を使う。式はそれに判定足と余裕を足した形。

> **ルール2に `InpR2_Tenkan` を含める**（2026-08-08・PR #10 のレビュー）。当初 `max(InpR2_Kijun, InpR2_SpanB)` と書いていたが、転換線の期間も計算に使う以上、外すと転換線を一番大きくしたときに不足を検出できない。既定値（9 < 52）では結果は変わらない。

## 2. 入力パラメータ

表示名は日本語にする（`docs/lessons_learned.md`）。`①②③④` の番号は `trading_rules.md` の条件番号と対応させ、診断出力を読んだときに即座に照合できるようにする。

### 共通

| 変数 | 既定 | 表示名 |
|---|---|---|
| `InpLots` | 0.10 | ロット |
| `InpMagic` | 20260808 | マジックナンバー |
| `InpReverseOnOpposite` | true | 反対シグナルでドテンする |

### ルール1: スーパーボリンジャー

| 変数 | 既定 | 表示名 |
|---|---|---|
| `InpR1_Period` | 21 | 期間（センターラインとσ） |
| `InpR1_LagBars` | 21 | 遅行線の本数 |
| `InpR1_UseSqueeze` | true | ①膠着を条件に入れる |
| `InpR1_SqueezeBars` | 21 | ①膠着とみなす本数（この本数ぶん帯の内側） |
| `InpR1_SigmaMult` | 3.0 | ①③で使うσの倍数 |
| `InpR1_UseExpand` | true | ④バンド幅の拡大を条件に入れる |
| `InpR1_ExpandBars` | 3 | ④拡大を見る本数 |
| `InpR1_Exit` | A | 決済の方式（A/B/C は `trading_rules.md` §4.2） |
| `InpR1_UseSL` | **false** | 損切りを使う |
| `InpR1_SLSigma` | 2.0 | 損切り（σの何倍） |
| `InpR1_UseTimeStop` | false | 保有本数で手仕舞う |
| `InpR1_HoldBars` | 24 | 手仕舞うまでの本数 |

③（遅行スパンの帯突破）は引き金なので ON/OFF を持たない。外すとルールが成立しない。`InpR1_SigmaMult` は①と③の両方に効く（同じ帯を物差しにするため）。

### ルール2: スパンモデル

| 変数 | 既定 | 表示名 |
|---|---|---|
| `InpR2_Enable` | false | ルール2を使う |
| `InpR2_Tenkan` | 9 | 転換線の期間 |
| `InpR2_Kijun` | 26 | 基準線の期間 |
| `InpR2_SpanB` | 52 | 長期スパンの期間 |
| `InpR2_LagBars` | 26 | 遅行線の本数 |
| `InpR2_UseLagClose` | false | ②a 遅行スパンが重なる足の終値を抜けている |
| `InpR2_UseLagHighLow` | true | ②b 遅行スパンが重なる足の高値安値を抜けている |
| `InpR2_UseLagCloud` | false | ②c 遅行スパンが重なる足の雲を抜けている |
| `InpR2_UseClosePos` | true | ③終値と青スパンの位置関係を条件に入れる |
| `InpR2_UseSlope` | true | ④長期スパンの傾きを条件に入れる |
| `InpR2_SlopeBars` | 5 | ④傾きを見る本数 |
| `InpR2_UseLagExit` | true | 遅行線の反転で手仕舞う |
| `InpR2_UseSL` | **false** | 損切りを使う |
| `InpR2_SLPips` | 20 | 損切り（pips） |
| `InpR2_UseTimeStop` | false | 保有本数で手仕舞う |
| `InpR2_HoldBars` | 24 | 手仕舞うまでの本数 |
| `InpEntryDelayBars` | 2 | エントリーを何本後の足の始値で出すか |
| `InpExitDelayBars` | 2 | 決済を何本後の足の始値で出すか |

**単体 EA `Experts\EaSpanModel.mq5` の変数名は接頭辞なし**（`InpTenkan` など・2026-08-19）。指標 `Indicators\SpanModel.mq5` と設定画面を1行ずつ見比べられるようにするため。`InpR2_` はルール1と同居させる EA を作るときに使う。同 EA に `InpR2_UseLagExit` 以降（遅行線での手仕舞い・損切り・保有本数）は無い。

**②と④はそれぞれ1項目にまとめてある**（2026-08-20・PR #18）。②は `InpLagMode`（なし / a / b / c / a+c / b+c の6択）、④は `InpSlopeMode`（使わない / 1〜5本）。別々の入り切りにすると、総当たりで**結果が完全に一致する組み合わせ**が並ぶため（②a と ②b の同時オンは ②b のみと一致、④を切ったときは本数を振っても全て一致）。ロット・マジックナンバー・診断出力・指標表示は `sinput` にして最適化の対象から外した。1280通りが実質288通りだった無駄はこれで消える。

①（雲の転換）は引き金なので ON/OFF を持たない。

**②は3つとも false にすると「②を使わない」になる。** ②専用の ON/OFF は置かない（同じことを2箇所で切り替えられると、どちらが効いているのか分からなくなる）。**忘れやすいので、3つとも false の状態で起動したらログに1行出す。**

**②a と②b を同時に true にする意味は無い。** 同じ足の高値は終値以上なので、b が成立すれば a は必ず成立する。検証する組み合わせは「なし / a / b / c / a+c / b+c」の6通り。

**「重なる足」＝遅行スパンの右端が乗っている足**（`InpR2_LagBars` 本ぶん前・既定 26）。**表示名に本数を書かない**（2026-08-17・PR #16 のレビュー）。設定項目の表示名は起動時に書き換えられないため、本数を変えると表示だけが嘘になる。データウィンドウの見出しは `PlotIndexSetString` で実際の本数を埋める。

### 診断

| 変数 | 既定 | 表示名 |
|---|---|---|
| `InpPrintCounters` | true | 条件別の成立回数を出力する |
| `InpShowIndicator` | true | チャートにスパンモデルを表示する（視覚モードのテスト・実チャートのみ。矢印と約定位置の突き合わせ用） |

### 表示名の付け方

方向に依存する言葉を使わない。「平らでも上向きとみなす」「+1σ 割れで手仕舞う」は買い目線であり、売りのとき意味が通らない。

| 避ける | 使う |
|---|---|
| ④平らでも上向きとみなす | ④判定にイコールを許可する |
| ②遅行線の陽転を条件に入れる | ②遅行線の陽転/陰転を条件に入れる |
| ③終値が青スパンの上を条件に入れる | ③終値と青スパンの位置関係を条件に入れる |

## 3. 執行の仕様

### エントリー

- 判定は確定足（`shift = 1`）のみ。ザラ場では判定しない（`trading_rules.md` §2.1）
- 発注は**次の足の始値で成行**。ただしルール2の単体 EA は `InpEntryDelayBars` 本後の始値（既定 2 = さらに1本待つ・`trading_rules.md` §5.2）
- **建玉は常に1つまで。** 保有中に同じ方向のシグナルが出ても無視する。積み増すと平均建値・建玉ごとの損切りが絡み、可変ロットを不採用にした理由（検証が複雑になる）とぶつかる

### 決済

- 手仕舞い条件（下記のX1系）は確定足で判定し、**次の足の始値で成行**
- **損切りだけは例外で、足の途中で発動する。** サーバー側の損切り注文なので、確定足を待たずに約定する。`trading_rules.md` §2.1 の「ザラ場判定はしない」は EA の**判定**についての決まりで、ブローカー側の逆指値の執行はこれに当たらない
- ルール1: `InpR1_Exit` で選んだ方式（既定は「終値が 1σ の内側へ戻る」）
- ルール2: 雲の色が反転したら手仕舞う。単体 EA は `InpExitDelayBars` 本後の始値で、遅行線の反転は持たない

### ドテン

**ドテンが実際に起きるのはルール2だけ。** 2つのルールで事情がまったく違う。

| | 手仕舞い条件 | 反対シグナル | 関係 |
|---|---|---|---|
| ルール2（買い保有中） | 雲が赤になる | 雲が赤へ転換した足 | **同じ出来事**。毎回ドテンになる |
| ルール1（買い保有中） | `InpR1_Exit` で選んだ方式 | 遅行スパンが −3σ を下抜け **＋ ①膠着 ＋ ④拡大** | **ほぼ無関係**。下記 |

ルール1の売りは遅行スパンの下抜けだけでなく①膠着（直前21本ぶん遅行スパンが ±3σ の内側）も要求する。買いを持っていたということは直前がトレンドなので①は成立せず、**手仕舞いだけが起きて反対建玉は入らないのが通常**。ドテンは例外的にしか発生しない。

同じ足で揃った場合は、次足始値で**決済と反対建玉を同時に行う**。`InpReverseOnOpposite` を false にすると決済のみ。**この入力が効くのは実質ルール2**。

### 損切り

**既定は使わない（`InpR1_UseSL` / `InpR2_UseSL` = false）。** 手仕舞い条件かドテンで抜けるのが基本設計であり、損切りの有無はバックテストで比較して決める。損切りは資料に記載がなく（`trading_rules.md` §4.2）、閾値を調整できるつまみを増やすほど学習期間の成績は実力以上に良く出る。

使う場合は**注文にサーバー側の損切り価格を付ける**。EA がティックを監視しない（§2.1 と整合する）、EA が停止しても効く、建玉の向きに応じた気配（買いは Bid、売りは Ask）を MT5 が自動で見る、の3点による。σ は建玉時の値を固定するため、損切り価格は建玉時に確定する。

> **売り側は Bid ではなく Ask。** 買い建玉は売って閉じるので Bid、売り建玉は買って閉じるので Ask を見る。`trading_rules.md` §4.2 の「売りは上下反転」はここを省いており、そのまま実装するとスプレッドのぶん売り側の損切りが甘くなる。サーバー側の損切りにすればこの誤りは起きない。

ブローカーの最小距離（`SYMBOL_TRADE_STOPS_LEVEL`）を下回る場合は、**最小距離まで広げて建てる**。見送りにすると σ が小さい場面（＝ルール1が狙う膠着明け）の取引だけが抜け落ち、成績が実態とずれる。広げた回数は診断に出す。

### SAR によるトレーリング（`trading_rules.md` §4.2 X4）

**新規注文を出し直すのではなく、建玉に付いている損切り価格を毎足書き換える**（`CTrade::PositionModify`）。M5 で保有中の足ごとに1回なので負荷は問題にならない。

- **建玉と同時に置く。** 次の足の更新まで待つと最初の1本だけ無防備になり、Bid 判定方式と条件が揃わなくなる
- **押し戻しと後退禁止は順番が決まっている。** 最小距離まで押し戻す → その結果が前回より緩ければ**書き換えを送らない**。押し戻しは緩める方向の操作なので、これが無いと価格が戻った場面で損切りが後退する
- **決済価格は約定履歴から引く**（`HistorySelectByPosition` → `DEAL_ENTRY_OUT` の `DEAL_PRICE`）。サーバー側で決済されると EA の外で建玉が消えるため、これが無いと比べたい方式の決済価格が 0 のままになり成績を出せない。`DEAL_REASON` で決済理由も判別し、CSV の `reason` に残す
- **最小距離の実測値を診断に出す。** 0 なら押し戻しは一度も起きない。ブローカーごとに違うので推測しない
- **σ の損切り（`InpR1_UseSL`）とは同時に使えない**（`OnInit` で弾く）。どちらも建玉の同じ損切り価格を書くため、後から置いたほうが黙ってもう一方を消す。CSV には両方の設定が残るので、結果から条件を再現できなくなる
- **損切り価格の算出は建玉時と更新時で同じ関数を通す。** 2箇所に書くと、片方だけ直したときに水準がずれる

## 4. 診断出力

`InpPrintCounters` が true のとき、テスト終了時（`OnDeinit`）に条件ごとの成立回数を出力する。

```text
[ルール1] ①膠着 812 / ③3σ突破 451 / ④拡大 5130
          3つ揃った 7   買い 4 / 売り 3
          損切りを最小距離まで広げた 0
          決済の方式: 終値が 1σ の内側へ戻った
```

**取引ゼロで終わったときに、どの条件で止まったかを切り分けるため。** 前回の破棄は「ルールが悪いのか実装が悪いのか分けられない」状態が尾を引いた。実装は数行で済む。

各条件は**単独で**数える（他の条件の成否と無関係にカウントする）。そうしないと「①が原因で②が数えられていない」といった連鎖が起き、切り分けにならない。

## 5. N2 で決めたこと

| # | 決定 | 理由 |
|---|---|---|
| 1 | 条件別の成立回数を出力する | 取引ゼロのとき切り分けられるようにする |
| 2 | 遅行線は厳格版（高値・安値と比較）のみ実装 | 緩い版（終値比較）では「絡む」という中間状態が作れず、資料が p13・p15 でレンジ判定に使っている状態が消える |
| 3 | ~~条件①を p15 の定義（±1σ 内推移）に置き換え~~ → **2026-08-09 に破棄**。①と③をどちらも「遅行スパンと帯の距離」で測る | p15 の定義は、同じ21本から作った帯と自分を比べる形なので何も測っていなかった。値動きを20倍にしても成立率が変わらない（`trading_rules.md` §3.3） |
| 4 | 長期スパンの傾きは**平らな足も通し、比較先は直近の全本数**。切替は持たず、選べるのは④を使うか否かと本数だけ（2026-08-16 / 17 に変更） | 長期スパンは階段状で平らな期間が長い。`>` では雲が転換した足の 43.3% が同値で落ち、④を通るのは 16.2%（人工データ30万本）。また端の1点だけと比べると、そこが谷なら**直近3本下がっている足も「上向き」**になる（チャートで実例を確認・全足の 0.8%） |
| 5 | 建玉は1つまで／反対シグナルではドテン | 積み増しは検証を複雑にする。ドテンは資料の「雲の色に従う」考え方に沿う |
| 6 | 損切りは既定 OFF、バックテストで比較 | 手仕舞い条件で抜けるのが基本設計。つまみを増やすと学習期間の成績が実力以上に出る |
| 7 | `.mqh` は指標ごとに2本 | 依存が自然に分かれ、N4 の「一度に両方作らない」作業順と一致する |

## 6. 初版で実装しないもの

| 項目 | 理由 |
|---|---|
| 手仕舞いに p13 の3条件を全部使う版 | バンド幅の「収束」の定義が要る。資料に定義がなく、決めるのは必要になってから |
| 遅行線の緩い版（終値比較） | ルール1では②ごと落とした（`trading_rules.md` §4.1）。ルール2で②を使うときに再検討する |
| ルール3（統合） | ルール1／ルール2 の単体成績を見てから（`trading_rules.md` §6） |

## 7. 参照

| 文書 | 内容 |
|---|---|
| `docs/trading_rules.md` | 売買ルールの要件定義。条件番号 ①②③④ の定義 |
| `docs/indicator_spec.md` | 2指標の計算定義 |
| `docs/backtest_design.md` | 期間3層、選択可能にする項目 |
| `docs/lessons_learned.md` | MQL5/テスターの罠、表示名の付け方 |
| `docs/backtest_results_spanmodel.md` | ルール2の学習期間の総当たり結果。固定してよい項目・外す項目 |
