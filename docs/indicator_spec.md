# インジケーター定義書 — スパンモデル / スーパーボリンジャー

> 対象: 柾木利彦氏（マーフィー）考案の「スパンモデル」「スーパーボリンジャー」。
> 本書は**描画・計算の定義のみ**を扱う。売買ルールは別途 `docs/` に起こす。
> 作成 2026-08-07。

## 出典

| # | 資料 | 得られたもの |
|---|---|---|
| S1 | [ヒロセ通商 テクニカル分析 #26](https://hirose-fx.co.jp/category/market/tec/content_tec/26.html) | 各シグナルの意味。**計算式・期間の記載なし** |
| S2 | [「勝つための方程式 スペシャル」PDF 全20頁](https://hirose-fx.co.jp/landing/seminar_lion/pdf/v_equationsp.pdf) | 売買判断の条件。遅行スパン=26 の明記（p17）。**その他の期間の記載なし** |
| S3 | Matrix Trader の指標設定画面（ユーザー提供スクリーンショット 2026-08-07） | **全パラメータの初期値**。本書の期間はすべてこれが根拠 |
| S3-2 | Matrix Trader でスパンモデルと一目均衡表を重ねた画面（同上、USD/JPY 5分足） | **スパンA の式の確定**、先行表示の有無の確定 |
| S3-3 | Matrix Trader でスーパーボリンジャーを表示した画面（同上、USD/JPY 5分足） | σ1/σ2/σ3 が同一 σ の整数倍であることの確定、遅行線21の確定 |
| S3-4 | 同上に単純移動平均線（期間21）を重ねた画面 | **TP ＝ 終値の21期間SMA の確定** |

## 1. スパンモデル

### 1.1 パラメータ（S3 の初期値）

| 要素 | 期間 | 既定色 |
|---|---|---|
| スパンA | 9 | 青 |
| スパンB | 52 | 赤 |
| 雲 | — | 雲色1 = 青 / 雲色2 = 赤 |
| 遅行線 | 26 | マゼンタ |

### 1.2 計算

```
転換線 = (Highest(High,  9) + Lowest(Low,  9)) / 2
基準線 = (Highest(High, 26) + Lowest(Low, 26)) / 2

スパンA = (転換線 + 基準線) / 2                        ← 一目均衡表の先行スパン1に相当
スパンB = (Highest(High, 52) + Lowest(Low, 52)) / 2    ← 一目均衡表の先行スパン2に相当
遅行線  = Close を 26 本前へずらして描画               ← 一目均衡表の遅行スパンと同一
```

UI のラベルは「スパンA[9]」だが、**実際には 26 も使う**（遅行線のパラメータ 26 が基準線の期間を兼ねている）。9 / 26 / 52 の3値で一目均衡表と同じ構成になる。

#### スパンA の式の確定根拠（2026-08-07）

一目均衡表を重ねた画面（S3-2）で、**転換線と基準線が乖離している足**にカーソルを合わせて検算した。カーソル位置 2026/08/07 00:35:

```
転換線[9]  = 158.332
基準線[26] = 158.140

候補A（スパンA = 転換線）           → 158.332
候補B（スパンA = (転換線+基準線)/2） → (158.332 + 158.140) / 2 = 158.236

実測 スパンA[9] = 158.236            → 候補B と小数第3位まで一致
```

同じ画面で `先行スパンA[26] 157.889` ≠ `スパンA[9] 158.236`、遅行線は両者とも 158.444 で一致。

> 注意: 転換線と基準線が同値になる足では両候補が同じ値になり判別できない。最初に撮った 2026/08/06 23:10 の足がこれに該当した（両方 158.040）。同種の検算を行うときは**2本が乖離している足を選ぶ**こと。

### 1.3 先行表示は行わない（重要）

**スパンA・スパンB を未来方向へずらさない。** 一目均衡表の雲は 26 本先行させて描くが、スパンモデルは現在足の位置に描く。

根拠3点:

1. S3 の設定画面に**先行本数のパラメータが存在しない**。
2. S2 p18 の図（5分足スパンモデル＋ボリンジャーバンド）を画素解析したところ、青・赤スパンの右端 x 座標が**ボリンジャーバンドの右端と完全に一致**した。ボリンジャーバンドは構造上ずらせないため、青・赤スパンもずれていないことになる。26 本先行していれば約 416px の差が出るはずだが、それが無い。
3. S3-2 で一目均衡表と同時表示したところ、**同じ足で値が違う**（`先行スパンA[26] 157.889` vs `スパンA[9] 158.236`、`先行スパンB[52] 157.879` vs `スパンB[52] 158.140`）。計算式が同じで値が違う以上、片方がシフトされている。チャート右端でも、最終足 12:40 に対し一目の雲だけが約26本ぶん先まで伸び、スパンモデルの帯は最終足で止まっていた。

同じ解析で、遅行線の終端が最終足より約 26〜28 本手前で切れていることも確認した（S2 p17 の「26遅行スパン」と整合）。

### 1.4 雲の色分け

```
スパンA > スパンB  → 雲色1（青）= サポートゾーン
スパンB > スパンA  → 雲色2（赤）= レジスタンスゾーン
```

## 2. スーパーボリンジャー

### 2.1 パラメータ（S3 の初期値）

| 要素 | 期間 | 既定色 | 備考 |
|---|---|---|---|
| TP（センターライン） | 21 | 青 | |
| σ1 | — | 緑 | |
| σ2 | — | 赤 | |
| σ3 | — | 水色 | |
| 遅行線 | **21** | マゼンタ | スパンモデルの 26 とは**別の値** |
| σ塗りつぶし | — | — | 既定 OFF |

### 2.2 計算

```
TP    = 終値の 21 期間単純移動平均（SMA）
σn上  = TP + n × σ
σn下  = TP − n × σ
遅行線 = Close を 21 本前へずらして描画
```

#### TP の確定根拠（2026-08-07）

S3-4 でスーパーボリンジャーに単純移動平均線（期間21）を重ね、カーソル 2026/08/07 00:25 で検算した:

```
スーパーボリンジャー  TP[21]    158.189
単純移動平均         期間C[21] 158.189   ← 完全一致
```

凡例の「期間**C**」の C は Close。**適用価格＝終値、平均種別＝SMA が同時に確定**した。

このカーソル位置は 21 本の窓が 22:40〜00:25 で急騰局面を丸ごと含む。Typical Price ベースなら終値が高値寄りに偏るぶん数 pips ずれるはずの区間で一致したので、偶然ではない。

> レンジ局面では終値ベースと Typical Price ベースがほぼ同値になり判別できない。同種の検算では**実体の大きい足が並ぶ局面を選ぶ**こと（スパンA のときと同じ注意）。

S3-3（スーパーボリンジャー単体を表示した画面。USD/JPY 5分足、カーソル 2026/08/07 00:10）で構造を検算した:

```
TP[21]    158.116
σ1        158.283 / 157.949   → ±0.167
σ2        158.450 / 157.782   → ±0.334          = 2 × 0.167
σ3        158.618 / 157.615   → ±0.502 / ±0.501 ≈ 3 × 0.167
遅行線[21] 158.497
```

3本とも同一の σ の整数倍で上下対称。**標準的なボリンジャーバンドの構造で確定**。σ ≈ 0.1672 円（16.7 pips）。

遅行線[21] も検算が合う。00:10 から21本後（＝01:55）の終値が約 158.50 で、凡例の 158.497 と一致する。**終値を 21 本前へずらしたもの**で確定。

**未確定な細部**（実装はブロックしない）:

- σ が標本標準偏差（÷ n−1）か母標準偏差（÷ n）か。MT5 の `iBands()` は母標準偏差

> **凡例の表示値だけでは判定できない。** 表示される σ は1つしかなく、それが「21で割った結果」か「20で割った結果」かを区別する相手がいない。両者の比は √(21/20) ＝ 1.0247、σ1 の幅にして 0.4 pips で、凡例の丸め（小数第3位）にも近い。
>
> **判定手順（未実施）**: スーパーボリンジャーの**期間を一時的に 2 に変更**すると、2流派の差が √2 ＝ 1.41 倍まで開く。2本の終値の差を D として、母標準偏差なら σ = D/2、標本標準偏差なら σ = D/√2。凡例の `TP[2]`（＝2本の終値の平均）と画面下部の OHLC 表示から D を復元すれば、スクリーンショット1枚で判別できる。
>
> **実務上の影響は小さい。** 「終値が +1σ を上回る」の判定が境界付近でごく稀に反転する程度で、優位性の有無を左右しない。既定は `iBands(21, 0, n, PRICE_CLOSE)`。

### 2.3 統合表示時のルール（S2 p17）

スパンモデルの上にスーパーボリンジャーを重ねる場合:

- スーパーボリンジャーは **±1σ と ±2σ のみ**表示（σ3 は出さない）
- 遅行線は**スパンモデルの 26 遅行スパンだけ**を表示（スーパーボリンジャーの 21 遅行線は出さない）

## 3. MT5 実装時の対応関係

| 本書の要素 | MT5 での取得 |
|---|---|
| スパンA / スパンB | **`iHighest` / `iLowest` から自前算出**（下記の理由により `iIchimoku()` は使わない） |
| 遅行線 | `Close[i+26]` との比較で判定（描画は `Close` を 26 本前へずらす） |
| TP / σ | `iBands(period=21)` |

> **スパンA・スパンB に `iIchimoku()` を使わないこと。** `iIchimoku()` の先行スパンのバッファは**すでに 26 本先行させた状態**で格納されており、現在足の位置にある値は 26 本前のデータから計算されたものになる。本指標が必要とするのは「現在のデータから計算した、先行させない値」で、それはバッファ上では 26 本**未来**の位置に当たるため `CopyBuffer` では取り出せない。高値・安値から直接計算すれば式は3行で済み、この落とし穴を丸ごと回避できる。

```
tenkan  = (Highest(High,  9, i) + Lowest(Low,  9, i)) / 2
kijun   = (Highest(High, 26, i) + Lowest(Low, 26, i)) / 2
spanA   = (tenkan + kijun) / 2
spanB   = (Highest(High, 52, i) + Lowest(Low, 52, i)) / 2
```

## 4. 未解決事項の一覧（N4 で決着させる）

- [x] ~~スパンA の式~~ → **確定**（先行スパン1 ＝ (転換線+基準線)/2）。§1.2 の検算を参照
- [x] ~~σ1/σ2/σ3 の関係~~ → **確定**（同一 σ の 1/2/3 倍、上下対称）。§2.2 の検算を参照
- [x] ~~スーパーボリンジャーの遅行線~~ → **確定**（終値を 21 本前へ）。§2.2 の検算を参照
- [x] ~~TP の基準価格と移動平均種別~~ → **確定**（終値の21期間SMA）。§2.2 の検算を参照
- [ ] σ の標準偏差の定義（標本 / 母）— 期間を 2 に変えた画面1枚で判別できる。手順は §2.2 参照。影響は 0.4 pips で実装をブロックしない

**スパンモデルは定義が完全に確定した。** スーパーボリンジャーも残るは σ の割り算の分母 1 件のみ。

いずれも Matrix Trader の実チャートと自作インジケーターを重ねれば判別できる。**数値の比較に入る前に、チャート上で形が一致することを目で確認する**（[[mql5-verification-honesty]]）。
