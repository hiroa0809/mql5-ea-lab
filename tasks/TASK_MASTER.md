# マスタータスクリスト

## 現在地: **次は P3（膠着メーターとスパンモデルの複合EA）から**。P1 は標準誤差の算出だけ残して保留、P2 は見送り（2026-08-20・ユーザー判断）。既存タスクは引き続き凍結

> **N5-7 以下の既存タスクには手を付けない。** 表示の道具（インジケーター4本）は揃ったので、ここからは**バックテストで数字を出す段階**に入る。凍結したタスクは、この4件が終わってから改めて要否を判断する。

> **打ち切りの根拠**: 条件を作り直し（0.90 → 1.93 pips）、決済を4通り、段階エントリー、パラボリック SAR まで試して、合否ライン 3.7 pips に一度も届かなかった。指標の実装ズレは目視と数値の両方で潰してあり、「実装が悪い」ではなく「この単体に優位性がない」と言える状態。
>
> **もともと資料の戦略は統合案（R3）であり、単体は検証のために分解した片方**（`docs/trading_rules.md` §2.3）。単体の不合格は資料の戦略の否定ではない。
>
> **上位足のスーパーボリンジャーに求めるのは「バンド幅が広がっているか」だけ**（ユーザー判断）。単体で試した決済方法・段階エントリーは今後スーパーボリンジャーでは使わない。スパンモデルで応用する可能性は残す。
>
> **退避先**: 売買プログラム本体と補助プログラム5本は `_archive/2026-08-14-rule1/`（Git 対象外）。実装はコミット `1629436` までの履歴にある。共有の計算とインジケーターは「帯・遅行線・拡大の判定」だけに絞った（コミット `7145114`）。
>
> インジケーターの確認は済んだ。ここから先の成績の数字は、指標の実装ズレが原因ではないと言える状態になっている。
>
> **PR #11・#12・#13・#15・#16 はすべてマージ済み。ブランチは `main` の1本だけ**（2026-08-18 時点）。次の作業は `main` から切る。
>
> **CodeRabbit の指摘を反映したあとの再レビューは行わない方針**（ユーザー判断・2026-08-12。レビュー回数に制限があり、待ち時間が長すぎるため）。
>
> **PR は積み上げない。** 未マージのブランチの上に次のブランチを切ると、`main` への PR に前の PR の内容が丸ごと入り、レビューが重複する（2026-08-12 に3段になり、#11 → #12 → #13 と順にマージして解消した）。

3指標（CCI/CMO/RSI）＋プライスアクションの構成は**全て破棄した**。RSI の売買反転・銘柄変更でもプラス収益に届かなかったため、ロジックの手直しではなく作り直しに切り替える。

次に開発するのは**スパンモデルとスーパーボリンジャーを使った EA**（2026-08-05 にユーザーが決定）。指標定義（`docs/indicator_spec.md`）・売買ルール（`docs/trading_rules.md`）・実装設計（`docs/implementation_design.md`）はすべて確定済み。**スパンモデルの判定は実装・目視確認まで完了（N5-2）。表示の道具も揃った（N5-8・PR #17 マージ済み）。次は上の「最優先タスク」P1 から。**

### 着手前に読む確定事実（再導出禁止）

> このブロックの記述が後の検証で否定されたら、**新しい記録を足すだけでなく、この行を消しに来ること**（[[retract-superseded-assumptions]]）。2026-08-07 に、否定済みの推測がこのブロックに残っていて事故りかけた。

- **前回の破棄理由は実装ミスではなくコスト前提の誤り**。往復スプレッドを10倍小さく見積もっていた（`USDCNH#` の実測 22 pips に対し、想定していたグロス優位性は 5.06 pips）。出典: `docs/lessons_learned.md`
- 検証の期間分割・回数制限は `docs/backtest_design.md`。**ロジックが変わってもこの設計は変えない**（IS 2018/11〜2022/12 ／ 検証用 2023/01〜2024/10 は2〜3回 ／ 最終OOS 2024/11〜 は1回だけ）
- 旧コードは `_archive/2026-08-05-legacy/`（Git 対象外・ローカルのみ）。**部品取り場**として温存しており、消していない
- **両指標の定義は全項目が確定済み**。`docs/indicator_spec.md` を読むこと。**推測で埋めない・再導出しない**
- **売買ルールも一意化済み**。`docs/trading_rules.md` を読むこと。**資料（S1/S2）の本文はこの文書に抽出済みなので再取得しない**。資料に無く Claude が決めた箇所は **【一意化】** と明記してあり、そこは変えてよい
- **MT5 の組み込み指標 `iIchimoku()` と `iBands()` は使わない。** 2026-08-05 時点で「これらで足りる」と書いていたが、2026-08-07 の検証で**両方とも否定された**:
  - `iIchimoku()` — 先行スパンのバッファは 26 本先行済みで格納されており、本指標が必要とする「先行させない現在値」は 26 本未来の位置に当たり `CopyBuffer` で取り出せない
  - `iBands()` — **母標準偏差**（n で割る）だが、Matrix Trader は**標本標準偏差**（n−1 で割る）。そのまま使うとバンド幅が 2.47% 狭くなる
  - どちらも**黙って違う値を返す**ため、気づかずに使うと目視確認でズレの原因究明に時間を溶かす。高値・安値・終値から自前算出する（合計数十行）
- **突き合わせの相手は Matrix Trader（USD/JPY 5分足）**。目視確認・検算はすべてこの画面と比べる
- **実装設計も済んでいる。`docs/implementation_design.md` を読むこと。** ファイル構成・入力項目一覧（日本語表示名つき）・執行の仕様・診断出力。**決定7件は同書 §5**。再設計しない
- **合否ラインは平均グロス損益 3.7 pips**（往復コスト 1.22 × 3）。**学習期間の安いコスト 0.69 で判定してはいけない**。出典: `docs/backtest_design.md`「期間別の取引コスト」
- **σ を事前に測る必要はない**（2026-08-08）。EA が実行時に計算する。「σ 16.7 pips の 22% だから事前ゲート通過」は根拠が無く取り下げ済み。**実装前に獲得値幅を見積もろうとしない**。出典: `docs/trading_rules.md` §4.3
- **時間帯フィルタは先回りして入れない。** サーバー時刻0時台のコストが 4.7 倍なのは実測済みだが、ルール1の発火時刻の分布を見てから判断する（出典: N0 の項）
- **MQL5 のビルド手順は `.claude/rules/mql5-build.md` にあり、`.mq5` / `.mqh` を開いたときだけ自動で読み込まれる**（2026-08-08 に動作確認済み。常時読み込みにはならない）

### 開発フロー（2026-08-05 合意）

```
0. コスト予算の確認        ← 実装前に必ず通す
1. 要件定義（売買ルールの一意化）
2. 設計書
3. タスク分解 → TASK_MASTER 登録
4. インジケーター実装 → チャートで目視確認
5. EA 実装
6. IS → 検証用 → 最終OOS
```

**ステップ0を先頭に置く理由**: 前回の破棄はロジックではなくコスト前提で決まった。対象銘柄・時間足を決めた時点で往復スプレッドを実測し、「狙う値幅が何 pips で、コストの何倍か」を先に出す。1倍を割るなら実装せずに条件を変える。

> **「保有が長くなるからコスト不利が小さくなる」は取り下げた（2026-08-07・N1b）。** 以前ここに「トレンドフォロー寄りで保有が前回（M15 × 24本＝6時間）より長くなる見込み」と書いていたが、N1b で見積もった R1 の想定保有は **M5 × 10〜60本（50分〜5時間）** で前回とほぼ同じか短い。**優位性は保有時間ではなく獲得値幅（σ のスケール）で確保する**必要がある。根拠は `docs/trading_rules.md` §4.3。

**ステップ4に目視を明示する理由**: 成果物は「動いた」ではなく「帯・遅行スパンの見え方が参照資料の図と一致している」こと。ここで認識がずれると後段の数字が全て無意味になる（[[mql5-verification-honesty]]）。

**設計書とタスク分解の順序**: 設計書が先。判定ロジックが確定しないとタスクの粒度が決まらず切り直しになる。「要件定義 ⇄ 設計書」の往復は可。

### 最大のリスク（要件定義で最も時間を使う所）

**スパンモデルもスーパーボリンジャーも、元は裁量判断の補助ツールであり、機械的なエントリールールが一意に決まっていない。** 「遅行スパンがローソクを上抜け」「価格が青帯の上」をどう組み合わせ、どのタイミング（確定足／ザラ場）で発火させるかは実装者が決める部分。ここの決め方で結論が変わる。

→ **要件定義の成果物は「if 文にそのまま落とせる文章」にする。** 曖昧なまま実装すると、負けたときに「ルールが悪いのか実装が悪いのか」を切り分けられなくなる。

### 実装方針（先に決めた設計判断）

- **判定ロジックは `.mqh` に置き、インジケーターと EA の両方から呼ぶ。** EA がカスタムインジケーターをハンドル経由で参照する作りにすると、テスターでの再計算が重くなりバグの切り分けも面倒になる
- ~~`_archive/2026-08-05-legacy/Include/Signals/ISignal.mqh` のインターフェース（`Init` / `Update` / `Entry`）はそのまま流用できる~~ → **初版では使わない**（2026-08-08・N2 の決定7）。ルールが2つの段階で抽象を挟んでも得るものがない。ルール3の形が決まってから必要なら導入する。出典: `docs/implementation_design.md` §1
- **単体で見てから組み合わせる。** 2つを AND で使うのは魅力的だが、前回は単体の赤字が確定しているのに AND 統合を最終目標に据えていた。まず片方ずつ IS にかける

### 最優先タスク（2026-08-19 設定・この4件を順にやる）

**狙いは、スパンモデルでエントリーし、スーパーボリンジャーで決済する形を作ること。** 膠着メーター（＝スーパーボリンジャーのスクイーズ）はエントリーの AND 条件として使い、決済は別に組む。

- [ ] **P1: スパンモデル単体を、時間足を変えてバックテストする**（保留・残りは標準誤差の算出のみ。次に着手するのは P3）
  - 時間足だけを変えて回す。判定条件は今のまま触らない
  - **完了条件**: 時間足ごとに「取引数・平均グロス損益・標準誤差」が並んだ表。合否ラインは**平均グロス 3.7 pips**
  - **多数の時間足を試した中の最良値を、単独で採用根拠にしない**。必ず標準誤差を添える（[[report-uncertainty-with-results]]）
  - **2026-08-20: 5つの時間足（5分/10分/15分/1時間/4時間）× 1280通りを実施済み**（PR #18）。結果は `docs/backtest_results_spanmodel.md`。**標準誤差だけ未算出のため完了にしていない** — 最適化レポートに1取引ごとの損益が無いため。出すには設定を1つ選んで単発テストを走らせ「バックテスト」タブのレポートを保存する
- [ ] ~~**P2: 優位性が出なければ、スパンモデルの判定を簡素化する**~~ — **見送り**（2026-08-20・ユーザー判断）。レンジで停滞することが確認できたため、簡素化ではなく P3 の複合へ進む。なお総当たりで条件の入り切りは全て試しており「④は残す価値あり・②③は時間足次第」という答えは出ている（`docs/backtest_results_spanmodel.md`）
  - 今の条件は4つ: ①雲の色が変わった ②遅行スパンが重なる足を抜けた ③終値と青スパンの位置 ④赤スパンの傾き
  - **減らす方向で試す。** どれが効いていて、どれが取引を減らしているだけなのかを見る
  - **完了条件**: 条件を減らした版が、P1 と同じ形の表で比較できること
- [ ] **P3: 膠着メーターとスパンモデルの AND 条件でエントリーを作り、バックテストする**（着手前に下の確定事実を読む・再導出禁止）
  - 膠着の測り方は**5通りのまま未確定**（`docs/trading_rules.md` §6.1）。**ここで数字を見て絞る**のがユーザー方針
  - 計算は `Include\Signals\Squeeze.mqh` にあり、EA からそのまま呼べる
  - **完了条件**: 測り方5通りぶんの成績表。どれを採るかの決定
  - **膠着は「そろそろ動く」しか教えない。上下の向きはスパンモデル側で決める**（査読研究に方向の予測力なし）
  - **膠着の測り方は選択式で実装する**（2026-08-20・ユーザー判断）。5通りを入力の選択肢として載せ、数字で絞る（[[keep-candidates-switchable]]）
  - **2026-08-20: 実装と第1段階の総当たりは完了。決定だけが残っている**（PR #19・未マージ）。5分/10分/1時間は720通り、15分は58通りを実測。結果は `docs/backtest_results_p3_squeeze.md`。**②時刻別と③ケルトナーのどちらを採るかが未決**（基準で答えが逆転する。下の確定事実を参照）
  - 次にやるなら: ②時刻別を10分足で、日足以外の測る足で深掘り／③ケルトナーの倍率1.5と期間21を振る（どちらも一度も振っていない）

  **確定事実（着手前に読む・再導出禁止・出典付き）**

  - **成績を支配しているのは「トレンドかレンジか」ではなく「1日にいくら動くか」。** 学習期間の前半38ヶ月（2018/11〜2021/12）は1日平均 0.622 円で29年間の最低クラス、2022年は 1.367 円で 2.2 倍。スパンモデル単体の利益はほぼ2022年だけで出ており、前半3年は残高が約4%減った。出典: `docs/regime_usdjpy_yearly.csv` / `docs/regime_usdjpy_monthly.csv`（2026-08-20 実測）
  - **これは膠着ロジックと緊張関係にある。** 膠着＝動きが小さい状態の検出であり、実測では動きが小さい期間は稼げていない。「膠着明けに動く」ことに賭ける設計になるため、**膠着中に建てて動かないまま削られる形になっていないか**を必ず確認する
  - **検証用期間（2023/01〜2024/10）はレンジ寄りではない。** 月内で3円超動いた月が 68%（学習期間の前半は 8%、2022年は 67%）。`docs/backtest_design.md` の「検証用はレンジ寄りの局面が主」は実測と食い違う。**検証期間で好成績が出ても「動かない相場で生き残れる」証明にはならない**。出典: `docs/regime_usdjpy_monthly.csv`
  - **④長期スパンの傾きは5つの時間足すべてで有効**（上位2割入りが オフ 0〜9% に対し オン 22〜23%、最大DDも全時間足でオンが小さい）。取引回数を揃えて比べても1回あたり利益が高いので「取引が減ってコストが浮いただけ」ではない。**②③は時間足で結論が逆転するので固定できない**。出典: `docs/backtest_results_spanmodel.md` §3・§6
  - **執行を遅らせる効果は時間足で反転する。** 短い足ほど遅らせるのが効き、1時間足以上では遅らせないのが効く。出典: 同 §6
  - **5分足は使えない。膠着を足しても変わらない。** 単体では72通り全部が赤字（最良 0.16 pips）。膠着を足した684通りでも**合格ラインに届いたのは0通り**、最良 1.27 pips で必要な 3.01 pips の半分以下。「フィルタで負ける取引を落とせば5分足も使えるのでは」という筋は否定された。出典: `docs/backtest_results_spanmodel.md` §5 ／ `docs/backtest_results_p3_squeeze.md` §3
  - **成績は「膠着なし」と比べる。単体の総当たりとは比べられない**（スパンモデル側の条件を全部切ってあるため）。15分足の膠着なしは 5年中4年が赤字・2022年を除くと −65,370円。膠着を足すと全8通りでここが改善した。出典: `docs/backtest_results_p3_squeeze.md` §7
  - **測り方は基準で答えが逆転する。** 平均的な成績なら**②時刻別**（3足中2足で最良。しかも日足で測る組が弾かれる不利つき）、時間足をまたいだ壊れにくさなら**③ケルトナー**（5分・10分・1時間の3つすべてで黒字になった唯一の形）。出典: `docs/backtest_results_p3_squeeze.md` §4・§6
  - **隣り合う2つの時間足で再現しても証拠にならない。** 10分足の最良（⑤値幅/日足126本）は15分足でも合格したが、5分足でほぼゼロ・1時間足で全部赤字だった。**3つ以上の時間足で確かめるまで「またいで再現した」と言わない**（[[adjacent-timeframes-are-not-independent]]）。出典: 同 §6
  - **1時間足の高い平均値を信用しない。** 合格ライン到達47%・1取引2.71 pips と最も良く見えるが、取引数の中央値158回・5年すべて黒字が0通り・上位3ヶ月が総利益の90%超。取引が少なくて数字がぶれているだけ。出典: 同 §2
  - **総当たり1回で全パターンの月別・年別が CSV に出る**（2026-08-20 実装・実行確認済み）。`<共有フォルダ>\<名前>_<銘柄>_<時間足>_opt.csv`。**設定を1つずつ選んで単発テストを回す必要はない**（58通りを1本ずつ回して非効率だった）。出典: `Experts/EaSpanModel.mq5` の `OnTester`/`OnTesterPass` ／ `docs/backtest_plan_p3_squeeze.md` §8
  - **設定ファイルの `Report=` は総当たりでは機能しない。** 指定してもファイルは作られず、テスターのログにも記録が残らない。順位・PF・最大DD が要るときは MT5 の画面から手で書き出す。出典: 2026-08-20 実測（`tools/run-backtest.ps1` 冒頭）
  - **MT5 はテスト後に自分を再起動することがある。** 連続実行では毎回閉じてから次を呼ぶ。出典: 2026-08-20 実測（テスト終了9秒後に通常セッションで起動していた）
  - **EA の入力構成が変わったので古い `.set` は使えない。** 遅行スパンは6択1項目（`InpLagMode`）、長期スパンの傾きは「使わない＋本数」1項目（`InpSlopeMode`）、ロット・マジック・診断出力・指標表示は `sinput` で最適化対象外。消えた項目は既定値で埋まるため、**起動時にログへ出る実際の設定を必ず読む**。出典: `Experts/EaSpanModel.mq5`（PR #18）
  - **`CTrade` の `Buy` / `Sell` / `PositionClose` が返す `true` は「要求が送られた」だけの意味。** 結果コードを見ないと成否は分からない（`Trade.mqh` を実確認）。出典: `Experts/EaSpanModel.mq5` の `TradeSucceeded()`
  - **15分足のバーは1999年から使える**（日足は1998年から）。ただし2018年10月以前はスプレッドが 0.20 pips 固定なので、使うならテスターで固定スプレッドを与える。出典: テスターのログ `USDJPY#,M15: history begins from 1998.01.02`（2026-08-20 実測）＋ `docs/backtest_design.md`
- [ ] **P4: スーパーボリンジャーを使った決済ロジックを検証する**
  - P3 で建てた玉の**決済**をスーパーボリンジャーで決める。狙いは膠着明けに伸びるぶんを取り切ること
  - 単体売買で試した決済3方式（終値が1σ内側 / 遅行スパンが1σ内側 / 遅行スパンが反対側）の実装は履歴にある（コミット `1629436` まで）。**ただし単体では3方式とも 0 と区別がつかなかった**ので、そのまま持ってこない
  - **完了条件**: 決済方式ごとの成績表と、採用する方式の決定

#### この4件を始めるときに読むこと

- **合否ラインは平均グロス損益 3.7 pips**（往復コスト 1.22 × 3）。学習期間の安いコスト 0.69 で判定しない。出典: `docs/backtest_design.md`
- **期間分割は変えない**（IS 2018/11〜2022/12 ／ 検証用 2023/01〜2024/10 は2〜3回 ／ 最終OOS 2024/11〜 は1回だけ）
- **`tools/run-backtest.ps1` は打ち切った売買プログラムを起動する形のまま。** P1 に入る前に書き換えが要る。MT5 の起動中判定・設定の書き漏れ対策は流用する価値がある
- **実ティックは 2022/01 から**。学習期間 50ヶ月のうち 38ヶ月は生成ティック。ただしスプレッドは1分足の実測値が使われる
- **CSV は Shift-JIS**。UTF-8 として読むと決済理由が化ける
- **グロス損益は「ネット＋(建玉時スプレッド＋決済時スプレッド)/2」で復元する**
- **表示の道具は main に揃っている**（PR #17・2026-08-19 マージ）:

  | ファイル | 役割 |
  |---|---|
  | `Indicators\SuperBollingerMTF.mq5` | 上位足の帯を下位足へ段状に重ねる |
  | `Indicators\SqueezeGauge.mq5` | 膠着メーター。測り方5通りを切替 |
  | `Indicators\DailyPivot.mq5` / `WeeklyPivot.mq5` | ピボット（金色／水色） |
  | `Include\Signals\Squeeze.mqh` | 膠着の測り方5通り。EA から呼べる |
  | `Include\Signals\HigherTf.mqh` | 上位足を下位足へ並べる位置決め |
  | `Include\Signals\Pivot.mqh` / `PivotDraw.mqh` | ピボットの式と並べ方 |

- **未検証**: ピボット2本は**チャートでの目視が未実施**。膠着メーターは目視の途中で③の不具合を1件見つけて直した段階。コンパイルは4本とも通っている

### タスク（**凍結中** — 上の4件が終わるまで着手しない）

- [x] **N1a: インジケーターの定義を確定**（2026-08-07 完了・**未解決事項ゼロ**）— `docs/indicator_spec.md`
  - 出典5件（ヒロセ通商の解説ページ / 「勝つための方程式 スペシャル」PDF 全20頁 / Matrix Trader の設定画面・一目均衡表重ね・SMA重ね・期間2）から起こした
  - スパンモデル: 転換線9・基準線26 から **スパンA = (転換線+基準線)/2**、スパンB = 52本の中値、遅行線 = 終値26本前。**先行表示しない**（一目均衡表との決定的な違い）
  - スーパーボリンジャー: **TP = 終値の21期間SMA**、**σ = 標本標準偏差（n−1）**、σn = TP ± n×σ、遅行線 = 終値21本前
  - 確定に使った手口は2つ。**同種の疑問が出たら同じ手が使える**:
    1. **定義の確定した標準指標を重ねて凡例の数値を突き合わせる**（一目均衡表でスパンA を、単純移動平均で TP を確定）
    2. **パラメータを極端な値にして候補間の差を拡大する**（期間を 2 にして σ の流派を確定。21 のままでは差が 1.02 倍で丸めに埋もれるが、2 なら 1.41 倍に開く）
  - **判別できない足を選ばないこと。** 転換線と基準線が同値の足ではスパンA の2候補が同値になり判定不能だった。レンジ局面では終値ベースと Typical Price ベースも同値になる
- [x] **N1b: 要件定義 — 売買ルールの一意化**（2026-08-07 完了）— `docs/trading_rules.md`
  - 出典 S1（ヒロセ通商ページ）/ S2（PDF 全20頁）の本文を実際に取得して条件を抽出した。**両サイトは通常の fetch が 403 を返す**。ブラウザ UA 付きの `curl` で取得でき、PDF のテキスト抽出は PyMuPDF（`import fitz`）で通る
  - 検証は **R1（SB 単体）→ R2（SM 単体）→ R3（統合 MTF）** の3段階に分けた。資料本来の戦略は R3 だが、単体成績が分からないまま統合すると切り分けられない
  - 資料が曖昧な箇所（遅行スパンの「陽転」「絡む」、バンド幅の「収束」「拡大」、4条件のどれをトリガーにするか、損切り）はすべて**【一意化】として明示**し、input で切替可能にする方針を書いた
  - **銘柄・時間足は暫定決定のまま（2026-08-07）: `USDJPY#` / M5 を主軸・M15 を比較対象**（R3 のみ H1 を併用）。ユーザーに3回確認して回答が無かったため Claude が決めた暫定であり、いつでも変更可
  - §8 に残していた未確定は **N0（往復スプレッド）と N2（`.mqh` の分割単位、膠着の閾値）で解消済み**。残るのは `InpSLSigma` と `InpSqueezeBars` の既定値で、いずれも IS で調整する
- [x] **N0: コスト予算の確認**（2026-08-07 完了・PR #7 / #8）— **往復コスト 1.22 pips / 必要グロス期待値 3.7 pips。優位性の有無は IS で判定する**
  - `USDJPY#` M5 の全94ヶ月（2018/11〜2026/08）を実測した。手数料 0.00（XM スタンダード口座）。**94ヶ月は計測した範囲であり、1.22 pips の母集団ではない**（1.22 は下記のとおり直近22ヶ月の平均）
  - **判定に使うのは直近（2024/11〜）の 1.22 pips。IS 期間の 0.69 は使わない。** コストは 2023年7月に段差で上昇し、以降3年間高い水準が続いている。IS の安いコストで合格させると検証用・最終OOS で必ず落ち、**残弾 2〜3 回の検証用を無駄に消費する**。詳細と根拠は `docs/backtest_design.md`「期間別の取引コスト」
  - **「σ（16.7 pips）の 22% だから事前ゲート通過」は取り下げた**（2026-08-08）。σ は1本読みで根拠が無い。**実装前に獲得値幅を見積もろうとしないこと**。前回型の事故（コスト 22 pips > グロス 5.06 pips）でないことは言えるが、優位性の有無は IS でしか出ない
  - **レイテンシコストは無視できる**（0.002 pips）。同一期間・同一銘柄で遅延なし／あり(241ms)を比較し、方式A が完全一致した状態で得た差。確定足判定・次足始値の成行という設計なら影響しない
  - **サーバー時刻 0時台（ロールオーバー）だけコストが 4.7 倍**（中央値 2.80 / p99 10.80 pips）。時間帯フィルタは R1 の発火時刻を見てから判断する
  - **実ティックの境界は 2018/10 の途中**。10月は合成（固定 0.2 pips）と実ティックが混在するため、**IS の起点を 2018/11 へ変更した**（`docs/backtest_design.md` に変更理由と実測根拠を記載）
  - **月別チェックには穴がある**: 月の途中で切り替わる場合、`min == max` にならないため `flat_months` が 0 と出る。**`min` が 0.20 の月は混在を疑う**（実ティック月の `min` は 0.60 以上）
  - **`min == max`（`flat`）は合成データの証明にならない**（2026-08-08・PR #8 のレビュー）。固定スプレッドを提示する実ティック期間でも成立する。判別材料は**値そのもの**（0.20 なら合成、0.60 以上なら実ティックの固定スプレッド）
  - 判定式は `docs/trading_rules.md` §4.3: **往復コスト × 3 ≤ IS の平均グロス損益（勝ち負けを含む全取引の平均）**。満たさなければ実装せず条件を変える

#### 計測ツール `Experts/SpreadProbe.mq5` の使い方（再計測が必要になったとき）

- 売買ロジックを持たない計測専用の EA。テスター以外では `OnInit` で起動を失敗させる（成行注文を出すため）
- **実行時の必須設定**: モデルは **「実際のティックに基づく全ティック」**、遅延は**遅延なし**。モデルを外すと固定スプレッドで埋まり計測が無意味になる。MQL5 にモデルを問い合わせる API が無いため事前に弾けず、事後の警告に頼る作り
- 出力は共有フォルダ `Common\Files` へ `n0_spread_<銘柄>_<時間足>_<実行スタンプ>_trades.csv` と `_summary.csv`。**上書きは起きない**（2026-08-07 に上書き事故を2度起こして対策済み。PR #8）。実行スタンプは `TimeLocal()` ＋ `GetTickCount()` で作るが、**どちらもテスターでは衝突しうる**（前者はシミュレーション時刻、後者は約16分40秒で一周）。最終的な一意性は `FileIsExist` で trades / summary の両方の実在を確認し、埋まっていれば連番を足すことで担保している
- **この計測は IS / 検証用 / 最終OOS の使用回数を消費しない**（戦略の成績ではなくコストの実測のため）。全期間を何度回してよい
- **読む順序**: ①`month` 行で `min` が 0.20 の月が無いか（合成の混入）→ ②`month_check` の `flat_months` → ③`tick_all` の中央値・p99 → ④`hour` 行で時間帯の偏り
- **方式Aと方式Bの一致は「測定の正しさ」の証明にはならない**（2026-08-07 に判明）。遅延なしで走らせると方式Bは定義上その瞬間の気配スプレッドと一致するため、差ゼロは半ばトートロジー。一致が保証するのは**注文→約定→履歴の経路が正しく動き、手数料もスリッページも乗っていないこと**まで
- **遅延あり／なしの比較は実施済み**（2026-08-07）。差 0.002 pips で**レイテンシは無視できる**と確定。同一期間・同一銘柄で、対照群である方式A が完全一致した状態で得た差のため信頼できる
- **本ツールは使い捨てではない。** `docs/backtest_design.md`「通貨ペア分割」で他銘柄のコストを測るとき、最終OOS の終了日を確定するときに再び使う。N0 が済んだからといって捨てない
- PR #8 でレビュー済み（2026-08-08）。指摘2件を反映: 実行スタンプの衝突（`FileIsExist` による確認を追加）と、`min == max` を合成データと断定していた警告文の修正
- [x] **N2: 設計書の作成**（2026-08-08 完了）— `docs/implementation_design.md`。ファイル構成（指標ごとに `.mqh` 2本）、入力パラメータ一覧、執行の仕様、診断出力。決定7件の一覧は同書 §5

- [x] **N3: タスク分解**（2026-08-08 完了）— N4 / N5 / N6 を工程ごとに割り、**各工程に「何をもって完了とするか」を付けた**
  - 決定（2026-08-08）: **インジケーターはスーパーボリンジャーから作る。** 最初にバックテストへかけるのがルール1（スーパーボリンジャー単体）のため、作った直後に成績検証へ進める。「スパンモデルを先に」（2026-08-07 合意）は取り消し

### N4: インジケーター実装（チャートで目視確認まで）

`.mq5` / `.mqh` を含むので feature ブランチ → PR → CodeRabbit。**一度に両方の指標を作らない**（ズレたとき原因の切り分けが面倒になる）。

- [x] **N4-0: 銘柄・時間足の決め方をユーザーに確認**（2026-08-08 完了）— **EA の設定項目にせず、貼り付けたチャート（テスターの指定）に従う**。`docs/trading_rules.md` §2.2 の文面もこれに合わせて修正済み。R3 の上位足だけは設定項目で持つ
- [x] **N4-1: `Include\Signals\SuperBollinger.mqh` — 計算**（2026-08-08 完了・`0 errors, 0 warnings`）
  - 公開している関数: `SB_Calc`（センターライン・σ・±1〜3σ をまとめて返す）／`SB_LagValue`（遅行線の描画値）／`SB_RequiredBars`（必要な履歴本数）
  - **終値配列は時系列順（添字0＝最新足）が前提。** 呼ぶ側が `ArraySetAsSeries(close, true)` を済ませること。逆順でもエラーは出ず値だけ狂う
  - `SB_Calc` / `SB_LagValue` は `bool` を返す。**戻り値を確認せずに使わない**（配列不足のまま添字を引くと実行時エラーで停止する）
- [x] **N4-2: `Indicators\SuperBollinger.mq5` — 描画**（2026-08-08 完了・目視確認まで実施）
  - Matrix Trader と並べて一致を確認: 価格の山谷（157.980 / 157.306）、線の構成5種、**遅行線が右端で21本手前に切れていること**、**ずらす向き**（急落の谷がローソク足より21本早い位置に出る）
  - **時間軸は6時間ずれる**（MT5 のサーバー時刻 対 日本時間）。次回の突き合わせでも換算が要る
  - **この確認では σ の割り方（n か n−1 か）は判別できない**。帯幅の差が 1.02 倍で線の太さに埋もれるため。判別は N4-3 で行う
- [x] **N4-3: スーパーボリンジャーの数値検算**（2026-08-08 完了）— **σ は標本標準偏差（n−1）で確定。両者一致**
  - 実測（日本時間 08/08 05:00 の足）: MT5 は 終値 157.594・センターライン 157.574 → 1本前の終値 157.554 → 差 0.040。予測 0.040÷√2 = **0.02828** に対し実測 0.02833（3σ から逆算）。母標準偏差の予測 0.02000 とは 1.4 倍違う
  - Matrix Trader も同じ手順で σ = 0.0248（予測 0.035÷√2 = 0.02475）。**両者とも n−1**
  - **2社の数値そのものは一致しない。** 同じ足の終値が 157.598（Matrix Trader）対 157.594（XM）と 0.4銭ずれる。**突き合わせは「各社の終値から計算した予測値」対「その会社の表示値」で行う**。他社比較のときは常にこの形にする
  - 副産物: 上下の線の中点が3組ともセンターラインに一致（対称性の確認）。遅行線の欄が空＝最新から21本以内で値が無い区間、を数値でも確認
- [x] **N4-4: `Include\Signals\SpanModel.mqh` — 計算**（2026-08-08 完了・`0 errors, 0 warnings`）
  - 公開している関数: `SM_Calc`（転換線・基準線・青スパン・赤スパン）／`SM_LagValue`（遅行線の描画値）／`SM_RequiredBars`
  - スーパーボリンジャー側と同じ約束: **時系列順の配列（添字0＝最新足）が前提**、`bool` を返すので**戻り値を確認せずに使わない**
- [x] **N4-5: `Indicators\SpanModel.mq5` — 描画**（2026-08-08 完了・**目視と数値の両方で一致を確認**）
  - 実装: 雲（青スパンが上なら青、赤スパンが上なら赤に塗り分け）、青スパン、赤スパン、遅行線の4種
  - **数値（日本時間 08/08 05:00 の足）**: 青スパン 157.530（Matrix Trader）対 157.526（XM）、赤スパン 157.465 対 157.461。**どちらも差 0.004 で、終値の差 0.004 と一致**。業者間の価格差そのものであり、同じ価格を入れれば同じ値が出る＝式が一致している
  - 目視3点とも一致: ①帯が最終足で止まっている（先へ伸びていない＝一目均衡表になっていない）②青と赤の切り替わる位置 ③遅行線が最終足の26本手前で切れている
  - データウィンドウの「雲」2行が青スパン・赤スパンと同値であることも確認（雲の塗りつぶしは別バッファを使うため）
  - **数値検算に期間の変更は不要だった。** スーパーボリンジャーでは σ の割り方を判別するため期間を2に落としたが、スパンモデルは高安の中値なので流派の分岐がなく、既定値のまま決着する
- [x] **N4-6: PR を出して CodeRabbit の指摘を反映**（2026-08-08 完了・PR #9 / #10）
  - PR #9（スーパーボリンジャー）: 指摘3件。うち Major 2件は実バグ（最古側の未代入・遅行線の先端が更新されない）
  - PR #10（スパンモデル）: 指摘1件。Major 1件（必要履歴本数に転換線の期間が入っていない）。**同じ誤りが `docs/implementation_design.md` の式にもあったため両方修正**。片方だけ直すと次に設計書を読んだとき同じ実装へ戻る

### N5: EA 実装

判定ロジックは N4 で作った共通ファイルへ追記し、インジケーターと EA の両方から呼ぶ。入力項目・表示名・執行の仕様は `docs/implementation_design.md` の表をそのまま使う（**再設計しない**）。

**着手前に読む確定事実（再導出禁止・出典付き）**

- **共通ファイルの約束事を守る。** ①価格配列は**時系列順（添字0＝最新足）が前提**で、呼ぶ側が `ArraySetAsSeries` を済ませる ②`SB_Calc` / `SB_LagValue` / `SB_LagOffset` / `SB_Rule1` / `SM_Calc` / `SM_LagValue` は `bool` を返すので**戻り値を確認せずに使わない**。どちらも破ってもエラーが出ず値だけ狂う。出典: `Include/Signals/SuperBollinger.mqh` 冒頭
- **インジケーターに描画を足すとき、PR #9 で受けた指摘2件を繰り返さない**（出典: `Indicators/SuperBollinger.mq5` の `OnCalculate`）:
  1. 初回計算は**全足を走査**し、値を出せない足に `EMPTY_VALUE` を入れる。飛ばすと MT5 側の初期値のまま線が引かれる
  2. 差分更新の範囲に**遅行線の先端（遅行本数の位置）を必ず含める**。含めないと足が増えても遅行線が伸びない。**表示直後は正常に見えるため目視では検出できない**
- **設計書の式をそのまま写す前に、入力項目の前提が埋め込まれていないか疑う**（2026-08-08・PR #10）。`docs/implementation_design.md` の必要履歴本数の式に転換線の期間が抜けており、実装が忠実に写した結果そのままバグになった。**指摘の出所が設計書なら設計書も直す**（[[fix-spec-not-only-code]]）
- **「終値と、同じN本から作った ±1σ を比べる」形は何も測っていない**（2026-08-09・N5-1）。帯は値動きが小さくなると一緒に細くなるため、値動きの大きさを20倍にしても成立率は 45.7% で変わらない（人工データ30万本）。**R3 のレンジ判定（`docs/trading_rules.md` §6）に同じ形が残っている**ので、着手時に §3.3 の `LagZ` へ置き換える
- **遷移する条件を2つ以上持たない。** 同じ足で揃うことがほぼ無く、取引がゼロになる。ルール1は引き金を③1つに絞った
- **2指標とも Matrix Trader と一致済み**（目視・数値の両方）。**ここから先で数字が合わなくても、指標の実装ズレを疑わない。** 出典: N4-3 / N4-5 の項
- **銘柄・時間足は EA の設定項目にしない**（チャート／テスターの指定に従う）。ユーザー合意済みで `docs/trading_rules.md` §2.2 も修正済み。蒸し返さない
- **目視確認はユーザー操作が必要。** Claude は MT5 の画面を見られず、**履歴ファイル（`bases\...\*.hcc`）も MT5 起動中は排他ロックで読めない**（2026-08-09 に確認）。形式も非公開なので解析に頼らない。インジケーターのデータウィンドウに条件の成否を出してあるので、スクリーンショットで突き合わせる

- [x] **N5-1: ルール1（スーパーボリンジャー）の判定を `SuperBollinger.mqh` へ追加**（2026-08-09 完了・ユーザーがチャートで目視確認）
  - 4条件: ①膠着（遅行スパンが直前21本ぶん ±3σ の内側）／②遅行線が直近21本の高安を抜け切っている／③遅行スパンが ±3σ を新規突破（引き金）／④バンド幅の拡大
  - **当初の定義（終値と ±1σ）は①③とも破棄した。** ①は何も測っておらず矢印がほぼ出ず、②は当時の定義（21本前の足1本と比較）だと③に対して100%重複していた。仕様書も同時に修正済み（`docs/trading_rules.md` §3.1/§3.2/§3.3/§4.1/§8、`docs/implementation_design.md` §1/§2/§5/§6）
  - 目視確認（2026/06/01 のスクイーズ→エクスパンション）: 発火後いったん3σの内側へ戻り、再度上抜けたところでエントリー。急騰に乗り遅れていない
  - **決済（X1）も同時に実装した**（`SB_Rule1Exit`）。エントリー側と違い**状態**で判定する（遷移にすると取り逃したとき決済されないまま走り続ける）。損切りと時間切れは資料に無く既定オフ
  - **決済は3方式から選ぶ**（`InpR1_Exit`）。**A = 終値が 1σ の内側へ戻る（既定・資料 p13）**／B = 遅行スパンが 1σ の内側へ戻る／C = 遅行スパンが反対側へ陰転する。B・C はエントリーと同じ物差しで、A だけは「今の帯」を基準にするためエントリーの勢いが強いほど決済のハードルが下がる。定義は `docs/trading_rules.md` §4.2
  - インジケーターは**建玉の有無を追う**（決済条件は7割以上の足で真になるため、保有中かを知らないと決済アイコンの意味が無い）。**印は判定した足に出す。実際の約定は次の足の始値なので1本ずれる**（条件の数値と同じ足に並べたほうが目視確認しやすいため）。保有中に出た同じ向きのサインは印を出さない
  - データウィンドウに①②③④と「遅行スパンの位置(σ)」を出しており、**矢印が出ない足でもどの条件で止まったか読める**
  - **チャートの印に白を使わない**（2026-08-09）。ローソク足の実体が白く塗られる配色だと同化して見えない。買いに関する印は足の下、売りに関する印は足の上へ揃えてある。矢印の太さは 5 が MQL5 の上限
  - 人工データでの保有本数（参考）: X1 は中央値6本。**TASK_MASTER が仮説に置いていた「10〜60本」より短い**。学習期間の実測で確認する
  - **1つのチャート例を見ながら条件を作り直した。過剰フィッティングの可能性は残る**（ユーザーも指摘）。**合否は N6-1（学習期間の平均グロス損益 3.7 pips 以上）で判定し、個別のチャート例を見て条件を足し引きしない**
- [x] **N5-2: ルール2（スパンモデル）の判定を `SpanModel.mqh` へ追加** — 完了 2026-08-18（PR #15 / #16）
  - ①雲の色の転換（引き金）／②遅行スパンの位置（終値・高安・雲の3通りを AND で選ぶ）／③終値と青いスパンの位置関係／④赤いスパンの傾き。定義は `docs/trading_rules.md` §3.1/§3.4/§5.2
  - 矢印・条件の内訳・条件の入切が効くことをチャートで目視確認済み
  - **決済は未実装**（売買プログラム本体を作るときに。`docs/trading_rules.md` §5.3）
  - 検証する組み合わせは **②6通り（なし/a/b/c/a+c/b+c）× ④5通り（なし/3/5/8/13本）＝30通り**。人工データ30万本での売買サイン回数は 6,647〜2,238 回で約3倍の幅

- [x] **N5-8: スパンモデルのチャートへ上位足のスーパーボリンジャーを重ねるインジケーター** — 完了 2026-08-19（PR #17）
  - 資料の統合表示は「スパンモデル上にスーパーボリンジャーを重ね、**±1σ と ±2σ のみ**表示。遅行線はスパンモデルの26のみ」（`docs/trading_rules.md` §1.7）
  - 完了条件（帯が段状に出ることを目視）は満たした。ユーザー提供のスクリーンショット3枚で確認
  - 当初の範囲を超えて、膠着メーター（測り方5通り）とピボット2本も同じ PR に入れた。**ピボット2本は目視が未実施**
  - **上位足の値の置き方は案A**（確定した上位足の値を、次の区間の本数ぶん横に並べる）。定義は `docs/indicator_spec.md` §2.4

  **確定事実（再導出禁止・出典付き）**
  - **雲もスパンも先行させない。** `SM_Calc(..., i, ...)` が返す値を**そのまま添字 i に描く**のが本プロジェクトの流儀で、一目均衡表のように26本先へずらさない。PR #16 で CodeRabbit がこれを一目均衡表と取り違えて Major の誤検知を出した。出典: `docs/indicator_spec.md` §1.3 / PR #16 のスレッド
  - **表示名に設定値（本数など）を書かない。** `#property` の文言は固定なので、設定を変えると表示だけが嘘になる。データウィンドウの見出しは `PlotIndexSetString` で埋める。**設定項目の表示名は起動時に書き換えられない**ので、数字を使わない言い方にする。出典: PR #16 の指摘
  - **インジケーターと EA のパラメータがずれると、無言で矢印の出方だけが変わる。** 突き合わせる前に両方の設定値を1項目ずつ確認する。前回は「④拡大を見る本数」が 8 と 3 で食い違い、色の問題と誤診した。出典: `docs/lessons_learned.md`
  - **差分更新では遅行線の先端を必ず計算範囲に含める。** 含めないと足が増えても線が伸びない。**表示直後は正常に見えるため目視では検出できない**。初回は全足を走査し、値を出せない足に `EMPTY_VALUE` を入れる。出典: `Indicators/SpanModel.mq5` の `OnCalculate` / PR #9 の指摘
  - **線の見え方**: 雲の塗りつぶしと同系色の線は**幅5**にしないと帯に埋もれて読めない（遅行線は3）。**印に白は使わない**（ローソク足の実体と同化）。買いに関する印は足の下、売りは足の上。矢印の太さは 5 が MQL5 の上限。出典: 2026-08-16 の目視 / N5-1
  - **定型チャートは手書きしない。MT5 の「定型として保存」で作らせる。** 描画1本につき `<graph>` ブロックが必要で、欠けると**エラーも出さずにそのインジケーターだけ読み込まれない**。テスターの可視化チャートが読むのは **`tester.tpl` という固定名のみ**。出典: `docs/lessons_learned.md`
  - **印は判定した足に出る。EA の建玉はその次の足。1本ずれるのが正しい。** 出典: 本ファイル N5-1 の項
  - **CodeRabbit の自動レビューは走らない**（スター10個未満のため手動起動が必要・制限は1時間に1回）。出典: `.claude/CLAUDE.md`
- [ ] **N5-7: 上位足で「大局」をどう判定するかを決める**（2026-08-14 追加）
  - 統合案の上位足に求めるのは「バンド幅が広がっているか」だけ（ユーザー判断）。共有の計算に `SB_Expanding`（拡大の判定）として実装済み
  - **未決の論点が3つある。1件ずつ決める**
    1. **上位足をどれにするか** — 資料は60分足（`docs/trading_rules.md` §1）。4時間足・日足も候補
    2. **「広がっている」を何本前と比べるか** — 既定の 8 本は15分足の単体売買で決めた値で、上位足で妥当かは未検証
    3. **向きをどう決めるか** — 広がっているだけでは上下が分からない。`docs/trading_rules.md` §6 の草案は「終値が +1σ より上なら上昇局面」としているが、**同じ形（終値と±1σの比較）は単体売買で『何も測っていない』と判明して破棄した定義**。そのまま採らない
- [x] **N5-3: `Experts\EaSpanBollinger.mq5` — 骨格**（2026-08-09 完了）— 1ヶ月（2022/12）で43取引が発生
- [x] **N5-4: 執行まわり**（2026-08-09 完了）— 決済理由が保有本数つきでログと CSV に出る
- [x] **N5-5: 診断出力**（2026-08-09 完了）— 条件別カウンタ＋取引ごとの CSV
- [x] **N5-6: PR を出して CodeRabbit の指摘を反映** — 完了 2026-08-12（PR #12 / #13）。指摘19件すべて妥当・誤検知ゼロ。1件のみ提案と別の直し方（そのまま直すと正常な実行を警告するため）
  - **判定・表示ぶんは PR #11 で完了**（2026-08-09）。指摘4件のうち3件を反映、1件（印を約定足へ）は理由を添えて見送り
  - **残りは EA・自動実行ぶん**。`feature/n5-ea-backtest` を PR #11 マージ後に main へ載せ替えてから出す

#### テスターの自動実行（2026-08-09 に整備・以降は手動で回さない）

- `tools/run-backtest.ps1` に設定ファイルを渡して MT5 を起動 → テスト → 自動終了。**MT5 を閉じてから実行する**（起動中だと設定が無視され、既存ウィンドウが前面に出るだけで「走っていないのに成功に見える」。スクリプトが先に検出して止める）
- モデルは既定「始値のみ」。足の始値でしか売買しないなら過不足がなく、**4年ぶんが数秒**で終わる。**損切りやトレーリングを使う場合はティックベースのモデルへ変える**（足の途中で発動するため）
- **このスクリプトは打ち切った売買プログラムを起動する形のまま**（2026-08-14）。次の売買プログラムを作るときに書き換える。残してあるのは、MT5 の起動中判定・設定の書き漏れ対策・出力の確認といった、痛い目を見て入れた仕組みを流用するため
- **実ティックは 2022/01 からしか無い**（2026-08-13 に実測。テスターのログに `ticks data begins from 2022.01.03` と出る）。学習期間 50ヶ月のうち 38ヶ月は生成ティックで走る。**ただしスプレッドは1分足に記録された実測値が使われる**ので、時間帯によるコストの変動は再現される。再現されないのは1分より短い急拡大だけ。`docs/backtest_design.md` は期間全体を実ティック前提で書いており、実態とずれている
- EA は `Common\Files` へ `r1_<識別名>_trades.csv` と `_summary.csv` を出す。**グロス損益は「ネット＋(建玉時スプレッド＋決済時スプレッド)/2」で復元する**（テスターの標準レポートは口座通貨の純損益しか出さない）
- **CSV は Shift-JIS**（MQL5 の `FILE_ANSI` は日本語 Windows でこうなる）。UTF-8 として読むと決済理由が化ける

### N6: バックテスト（`docs/backtest_design.md` の3層に従う）

**合否は平均グロス損益 3.7 pips 以上**（往復コスト 1.22 × 3）。学習期間の安いコスト 0.69 で判定しない。

- [x] **N6-1: 学習期間（2018/11〜2022/12）でルール1を単体で回す**（2026-08-09 完了）— **不合格**
  - `USDJPY#` M5・310,091足を判定。決済方式 A/B/C の3通りを同一期間で実行

  | 決済方式 | 取引 | 平均グロス | 標準誤差 | 平均ネット | 勝率 | 平均保有 |
  |---|---|---|---|---|---|---|
  | A 終値が1σの内側へ | 2,104 | 0.29 pips | ±0.32 | −0.59 | 35.9% | 6.9本 |
  | B 遅行スパンが1σの内側へ | 2,099 | 0.75 pips | ±0.48 | −0.17 | 34.3% | 25.9本 |
  | C 遅行スパンが反対側へ | 2,051 | 0.90 pips | ±0.54 | −0.04 | 34.0% | 36.4本 |

  - **3方式とも 0 と区別がつかない**（最良の C で 1.7 標準誤差）。「わずかにプラス」ではなく「プラスかどうか判定できない」が正確
  - **ネットは3方式とも赤字**。学習期間の安いスプレッドですらこうなので、**グロスが同程度なら**コストが上がった直近期間では平均ネットはさらに下がる。ただし直近期間ではシグナルもグロス損益も変わるため、**「確実に悪化する」とまでは言えない**（N6-4 は未実施）
  - **保有を伸ばすとグロスは改善するが、勝ちと同時に負けも伸びる**（負けの平均 −4.6 → −8.1 → −9.5 pips）。改善幅は 3.7 pips に遠く及ばない
  - 年別では **2022年だけ突出**（C で 3.65）。ドル円が115→150円へ一方向に動いた年で、1年を取り出して合格と見なさない
- [x] **N6-1b: ルール1の条件見直し**（2026-08-12 完了）— **0.90 → 1.93 pips。合否ライン 3.7 には届かず**

  **最良の設定**: 15分足・決済方式C（遅行スパンが反対側へ陰転）・②遅行線なし・①膠着21本・④拡大8本。933取引で平均グロス 1.93 pips（±1.26）、平均ネット +1.15 pips

  **② 遅行線の陽転/陰転は削除した（コード・仕様書とも）**
  - 最適化1000パス（15分足・学習期間）の対比較で、**496組すべてが「入れないほうが良い」で一致**（平均 +0.32 pips、決済Cに限ると +0.53 pips）
  - **②の定義そのものは `docs/trading_rules.md` §3.1 に残す**。ルール2（スパンモデル）が使うため

  **RSI 決済（買いは RSI 80以上、売りは 20以下で降りる）も削除した**
  - 対比較 120組すべてが「使わないほうが良い」で一致（+0.56 pips、決済C・②なしに限ると +1.12 pips）
  - 保有を大きく縮めており（例 20.7本 → 9.7本）、勝ちを取り切る前に降りていた

  **①膠着は 21本が妥当。変える必要はない**
  - 本数を 5 / 10 / 21 / 31 / 42 / 63 と振ると、成立率が 52% → 1.4% まで滑らかに動く。**「変えても何も起きない軸」ではない**
  - 15分足・5分足 × ④拡大 3本・8本 の**4通りすべてで、21〜42本の帯が「切る / 5本 / 10本」を上回った**
  - **63本は採用しない。** 15分足 −2.45 / 5分足 +1.81 と符号が逆転する。取引が84〜301本まで落ち、標準誤差 1.3〜2.9。サンプルが崩壊した領域の揺れ
  - ④拡大は**残す**。全体では差がない（−0.03）が、決済C・②なしに限ると入れたほうが 0.29 pips 良い

  **段階エントリー（装填1 → 装填2 → 発火）は、装填42本では効果なし。既定 Off で温存**

  > **「段階エントリーは効果なし」と一般化しないこと。** 測ったのは**装填が生きている本数＝42本だけ**。2026-08-12 に一度その断定を書いて訂正した。同日の1000パスの中に、**装填72本で平均グロス 2.70 pips（557取引・ネット +1.87）** の1本がある（`r1_smoke_summary_999.csv`）。段階なしの最良 1.93 を上回るが、標準誤差 1.72 で95%区間 −0.67〜6.08、しかも1000パス中の最上位1本なので**採用判断はできない**。**次に段階エントリーを扱うときは、まず装填の本数（42/52/62/72/84）を振る**

  - 狙い: 膠着から拡大へ向かう1回目のサインはダマシで振られるので見送り、損切り相当を確認してから ±3σ 抜けで入る
  - **装填42本・5分足では14通りすべてが、段階なしより悪化**
  - 決定的だったのは**勝率が変わらないこと**。取引を 2051 → 631 と69%削減しても、勝率は 34.0% → 33.9% のまま。**勝ち負けに対して情報を持たない絞り込み**で、無作為に3分の1を選んだのと区別がつかない
  - 装填42本・15分足では見かけ上改善する組み合わせが出たが（最良 1.45）、95%区間がすべて 0 をまたぐ。同じ入口で出口設定を変えただけで +1.32 → −1.54 と符号が反転する例もあり、サンプル不足の揺れ

  **まだ振っていない設定**: 損切り（使う／σ倍率）、保有本数で手仕舞う（使う／本数）、反対シグナルでドテンする。いずれも全パスで既定値のまま固定していた

  > 段階エントリーは**上のとおり別途 30通りで測っており、未検証ではない**。ユーザーが回した1000パスの最適化で振られていなかっただけ。ただし**装填が生きている本数（42本固定）と RSI の期間（14固定）は一度も振っていない**

  **確定事実（再導出禁止・出典付き）**
  - **約定時の往復コストは 0.88〜0.94 pips**。N0 が測った学習期間の平均 0.69 pips の**約1.3倍**。ルール1がボラティリティの立ち上がりで発火するため。`docs/trading_rules.md` §4.3 の「×3 の根拠」2番目が実測で裏付けられた形。出典: 今回の trades CSV（グロスとネットの差で裏取り済み）
  - **人工データの見積もりは実データとよく一致した**。条件別の成立率は ①25.0%（予測 24.4%）②10.6%（13.6%）③4.0%（4.3%）④48.2%（49.9%）、平均保有 7.2本（予測 6.8本）。**方式の当たりを付ける段階では人工データで足りる**
  - **指標の実装ズレは疑わない。** Matrix Trader と目視・数値の両方で一致済み（N4-3 / N4-5）。条件別カウンタも想定どおり。**「実装が悪い」ではなく「このルールに優位性がない」と言える状態**
  - **資料本来の戦略は R3（統合）であり、ルール1単体は資料に無い形**。検証のために分解した片方なので、単体が不合格でも「資料の戦略が駄目」という結論にはならない。出典: `docs/trading_rules.md` §1.1・§2.3
- [x] ~~**N6-1c: 段階エントリーの「装填が生きている本数」を振る**~~ — **廃止**（2026-08-14）。スーパーボリンジャー単体の成績を上げるための作業で、単体そのものを打ち切ったため。段階エントリーの実装も削除済み

  **確定事実（再導出禁止・出典付き）** — 段階エントリーをスパンモデルで再利用するなら、まずこれを読む
  - **「段階エントリーは効果なし」は取り下げ済み。** 測ったのは装填42本だけ。1000パス中に**装填72本で平均グロス 2.70 pips（557取引・ネット +1.87）**の1本がある。出典: `r1_smoke_summary_999.csv`、N6-1b の項
  - ただし **2.70 は採用根拠にならない**。標準誤差 1.72・95%区間 −0.67〜6.08 で 0 をまたぐ。**1000パス中の最上位1本**であり、実力ゼロでも約17回に1回はこの数字が出る（1000パスなら60本前後）。出典: 同上の trades CSV で実測
  - 1取引あたりのばらつきは **40.7 pips**（平均 2.70 の15倍）。**上位10件を除くと平均がマイナスに転落**し、その10件が総利益の134%を占める。少数の大勝ち依存はトレンド追随として設計どおりだが、判定には約870取引が要る
  - **テスターは、設定ファイルに書かれていない入力の「前回値」を引き継ぐ。** ソースの初期値ではない。2026-08-12 に④拡大の本数が 8 のまま引き継がれ、取引数が 2104 → 1918 とずれた。`tools/run-backtest.ps1` は結果に影響する入力を全部書くよう修正済み。**手動でテスターを回した後に自動実行するときは特に注意**
- [ ] **N6-2: 売買サインが出た時刻の分布を見て、時間帯で絞るかを決める** — サーバー時刻0時台はコストが 4.7 倍（中央値 2.80 / p99 10.80 pips）。**先回りして入れない**。スーパーボリンジャー単体の分は測らずに終わったが、**時間帯でコストが変わるという実測は統合案でも効く**。スパンモデルの取引明細（建玉時刻つき）が出た時点で、追加実行なしで分布を出せる
- [ ] **N6-3: 学習期間でスパンモデルを単体で回す** — 完了条件は N6-1 と同じ（平均グロス 3.7 pips 以上）。**統合案ではスパンモデルがエントリー役**なので、単体の性質を知る意味は残る。ただし**単体で合格しなくても打ち切らない**。上位足のフィルタが付く前提の部品であり、単体の成績はそのまま採否にならない
  - **比べる組み合わせは30通り**（②6通り × ④5通り。`docs/trading_rules.md` §5.2）。**結果には取引数と誤差の幅を必ず併記する。** 平均グロス損益は取引数で割った値なので、条件を厳しくして取引を減らすほど良く見えやすい（ユーザー指摘・2026-08-17）。出典: [[report-uncertainty-with-results]]
  - **確定事実（再導出禁止・出典付き）**
    - **実ティックは 2018/11 から使える。学習期間50ヶ月はすべてクリーン。** `Experts/SpreadProbe.mq5` で全51ヶ月を月別に実測し、合成データ由来の固定スプレッド（min = 0.20 pips）が出るのは 2018/10 のみだった。出典: `docs/backtest_design.md`「IS の起点を 2018/10 → 2018/11 へ変更した理由」（2026-08-07 実測）。**以前ここに書いていた「実ティックは 2022/01 からしか無い」は 2026-08-20 に取り消した**（ユーザー判断・上の実測を採る）
    - **ブローカーの最小距離（`SYMBOL_TRADE_STOPS_LEVEL`）は 0 ポイント**（XM の `USDJPY#`）。損切りを置く位置の制限は実質かからない。出典: 2026-08-13 の実測（診断出力）
    - **パラボリック SAR は決済として効かなかった。** 5通りすべて従来（平均グロス 1.93 pips）を下回る 1.36〜1.58。保有が半分以下に縮み、勝率は上がるのに平均損益が下がる（勝ちを取り切る前に降りる）。**スパンモデルで再利用を考えるなら、この形になる前提で設計する。** 出典: PR #14
- [ ] **N6-4: 検証用期間（2023/01〜2024/10）** — **2〜3回まで**。使い切ったら終わり
- [ ] **N6-5: 最終確認用期間（2024/11〜）** — **1回だけ**

## 参照

| 文書 | 内容 |
|---|---|
| `docs/indicator_spec.md` | スパンモデル／スーパーボリンジャーの計算定義・パラメータ・出典・確定根拠の検算。**未解決事項ゼロ** |
| `docs/trading_rules.md` | 売買ルールの要件定義。R1/R2/R3 のエントリー・決済条件、用語の一意化、優位性の予算。**【資料】/【一意化】/【不採用】で出典と判断を区別** |
| `docs/implementation_design.md` | 実装設計。ファイル構成、入力パラメータ一覧（日本語表示名つき）、執行の仕様、診断出力。**N2 の成果物** |
| `docs/backtest_design.md` | 期間3層（IS / 検証用 / 最終OOS）、データ制約、通貨ペア分割。**ロジック非依存** |
| `docs/lessons_learned.md` | 破棄した試行から得た一般則、MQL5/テスターの罠、CodeRabbit 運用。**ロジック非依存** |
| `.claude/CLAUDE.md` | Git 運用ルール、MQL5 ビルド手順、ジャンクション構成 |

## 環境（維持済み・作り直し不要）

- [x] A1: MQL5 コンパイル環境（2026-07-26）— XM の MetaEditor を CLI 呼び出し。ジャンクションでリポジトリを MT5 データフォルダに接続。手順は `.claude/CLAUDE.md`「MQL5 ビルド」
- [x] A2: 開発フロー用スキル（2026-07-26）— `go` / `fin` / `commit-pr` / `coderabbit-review`
- [x] A3: CodeRabbit の MQL5 対応検証（2026-07-26 PR #1）— 対応を確認。詳細は `.claude/CLAUDE.md`

> ジャンクション3本（`Experts` / `Include\Signals` / `Indicators`）はリポジトリを指したまま有効。参照先が空になっただけで、新ロジックの実装時にそのまま使える。

## 流用候補（`_archive/2026-08-05-legacy/` に温存）

次のロジックが決まってから、必要な部分だけ引く。**先回りして汎用テンプレート化はしない**。

| 元ファイル | 流用できる部分 |
|---|---|
| `Experts/EaPriceAction.mq5` | 逆指値エントリーの予約・N本以内キャンセル、ストップレベルの最小距離丸め（`NormalizeStopLevel`）、ヘッジ口座での建玉特定（`CurrentPositionDir`）、取引要求の結果コード確認（`ReportTradeResult`）、N本タイムストップ、新規足判定、シグナル描画 |
| `Include/Signals/ISignal.mqh` | シグナル部品の共通インターフェース（`Init` / `Update` / `Entry`）、クロス判定ヘルパ |
| `Experts/EaLab.mq5` | 売買反転・片側テスト・決済方式の input 切替の作り |

破棄したロジック本体（`PriceAction.mqh` のパターン検出、`SignalRSI.mqh`、`SignalCMO.mqh`、`PriceActionViewer.mq5`、旧仕様書、バックテスト結果 xlsx、前プロジェクトの research データ）も同じ場所にあるが、**結論が無効なので流用しない**。
